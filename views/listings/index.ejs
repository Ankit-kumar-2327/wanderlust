<%- layout("/layouts/boilerplate") %>
<body>
    <style>
        #filters {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
        }
        .filter{
            text-align: center;
            margin-right: 2rem;
            margin-top: 1rem;
            opacity: 0.7;
        }
        .filter:hover{
            opacity: 1;
            cursor: pointer;
        }
        .filter p {
            font-size: 0.8rem;
        }
        .listing-card {
            position: relative;
        }
        .listing-heart {
            position: absolute;
            top: 0.8rem;
            right: 0.8rem;
            color: #ff385c;
            font-size: 1.2rem;
            text-shadow: 0 1px 2px rgba(0, 0, 0, 0.25);
        }
    </style>
    <div id="filters" class="mb-3">
        <div class="filter">
            <div><i class="fa-solid fa-fire"></i></div>
            <p>Trending</p>
        </div>
        <div class="filter">
            <div><i class="fa-solid fa-bed"></i></div>
            <p>Rooms</p>
        </div>
        <div class="filter">
            <div><i class="fa-solid fa-mountain-city"></i></div>
            <p>Iconic cities</p>
        </div>
        <div class="filter">
            <div><i class="fa-solid fa-person-swimming"></i></div>
            <p>Amazing pools</p>
        </div>
        <div class="filter">
            <div><i class="fa-solid fa-mountain"></i></div>
            <p>Mountains</p>
        </div>
        <div class="filter">
            <div><i class="fa-solid fa-snowflake"></i></div>
            <p>Arctic</p>
        </div>
        <div class="filter">
            <div><i class="fa-solid fa-ship"></i></div>
            <p>Boats</p>
        </div>
        <div class="filter">
            <div><i class="fa-solid fa-tent-arrow-down-to-line"></i></div>
            <p>Camping</p>
        </div>
        <div class="filter">
            <div><i class="fa-solid fa-cow"></i></div>
            <p>Farms</p>
        </div>
    </div>
    <br>
    <% if (typeof searchTerm !== 'undefined' && searchTerm) { %>
        <div class="text-center mb-3">
            <h5>Showing results for: <span class="text-danger"><%= searchTerm %></span></h5>
        </div>
    <% } %>
    <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-4 g-3">
        <% for(let listing of allListings){ %>
            <a href="/listings/<%= listing._id %>" class="listing-link">
                <div class="card col listing-card">
                    <i class="fa-solid fa-heart listing-heart"></i>
                    <img src="<%= listing.image.url %>" class="card-img-top" alt="img-listing">
                    <div class="card-img-overlay">Guest favourite</div>
                    <div class="card-body">
                        <p class="card-text">
                            <b><%=listing.title%> </b><br>
                            &#8377;<%= listing.price.toLocaleString("en-IN")%>/night
                        </p>
                    </div>
                </div>
            </a>
        <% } %>
    </div>
</body>